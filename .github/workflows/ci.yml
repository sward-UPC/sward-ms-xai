name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    uses: sward-UPC/.github/.github/workflows/ci-microservice.yml@main
    with:
      needs_shared: true
